import React from 'react';
import { BrowserRouter, Switch, Route }from 'react-router-dom';

function routes() {
    return (
        <BrowserRouter>
            <Switch>
                <Route />
            </Switch>
        </BrowserRouter>
    )
}

export default routes
